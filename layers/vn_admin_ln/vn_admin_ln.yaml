layer:
  id: "vn_admin_ln"
  description: description
  buffer_size: 4
  fields:
    code:
    name:
    name_en:
    level:
    level_num:
    adminleft:
    adminright:
  datasource:
    geometry_field: geometry
    query: (SELECT geometry, code, name, name_en, level, level_num, adminleft, adminright FROM layer_vn_admin_ln(!bbox!, z(!scale_denominator!))) AS t
schema:
  - ./vn_admin_ln.sql

