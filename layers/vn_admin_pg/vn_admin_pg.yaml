layer:
  id: "vn_admin_pg"
  description: description
  buffer_size: 4
  fields:
    code:
    name:
    name_en:
    level:
    level_num:
    color:
  datasource:
    geometry_field: geometry
    query: (SELECT geometry, code, name, name_en, level, level_num, color FROM layer_vn_admin_pg(!bbox!, z(!scale_denominator!))) AS t
schema:
  - ./vn_admin_pg.sql

