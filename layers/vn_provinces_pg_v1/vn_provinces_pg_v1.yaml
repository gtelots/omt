layer:
  id: "vn_provinces_pg_v1"
  description: description
  buffer_size: 4
  fields:
    code:
    name:
    name_en:
    level:
    admin_level:
  datasource:
    geometry_field: geometry
    query: (SELECT geometry, code, name, name_en, level, admin_level FROM layer_vn_provinces_pg_v1(!bbox!, z(!scale_denominator!))) AS t
schema:
  - ./vn_provinces_pg_v1.sql